package com.pluralsight;

public class ReceiptFileManager {

    // Saves receipts to file. i.e my file reader/writer

    // Each order should have its own receipt file:-

    // File folder = new File("receipts"); ???
    // if (!folder.exists()) {
    //      make a new folder
    // }
}
